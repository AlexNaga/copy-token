{
  "name": "copytoken",
  "version": "1.0.0",
  "description": "A puppeteer script for copying a token to your clipboard.",
  "main": "./app.js",
  "bin": {
    "copytoken": "./app.js"
  },
  "scripts": {
    "start": "node ./app.js",
    "postinstall": "cp -n .env.example .env.test && cp -n .env.example .env.prod || exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "clipboardy": "^2.3.0",
    "dotenv": "^9.0.2",
    "puppeteer": "^9.1.1"
  },
  "devDependencies": {
    "prettier": "2.3.0"
  }
}
